name: kakeibo-ai
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - pyzaim
      - google-generativeai
      - python-dotenv
      - requests
      - slack-sdk
      - playwright
      - pandas
