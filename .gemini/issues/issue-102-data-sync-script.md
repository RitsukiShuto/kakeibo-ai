# Issue: 匿名化機能付きデータ同期スクリプトの作成

## 概要
本番環境のデータベースをステージング環境へ同期する際、個人情報を匿名化するスクリプト `tools/sync_prod_to_staging.py` を作成する。

## 背景
ステージング環境で本番に近いデータを使って検証したいが、セキュリティとプライバシーの観点から、生の支出明細（店舗名やメモ）をそのまま共有するのは避けるべきである。

## 作業内容
- [ ] `tools/sync_prod_to_staging.py` の実装。
- [ ] `local/kakeibo.db` を `staging/local/kakeibo_staging.db` へコピーする。
- [ ] **重要要件**: スクリプトがターゲットとするディレクトリが `staging/local/` であることを厳格にチェックし、本番DBを誤って上書き・破損させないためのガードレールを実装する。
- [ ] `transactions` テーブル等の `content`, `description` カラムを匿名化（例: 固定のプレースホルダーやハッシュ化）する。
- [ ] 金額、カテゴリID、日付などの集計に必要な情報は維持する。

## 担当エージェント
- @backend-engineer

## ステータス
- [ ] 未着手
- [ ] 進行中
- [ ] 完了
