# Issue: 匿名化機能付きデータ同期スクリプトの作成

## 概要
本番環境のデータベースをステージング環境へ同期する際、個人情報を匿名化するスクリプト `tools/sync_prod_to_staging.py` を作成する。

## 作業内容
- [x] `tools/sync_prod_to_staging.py` の実装を完了。
- [x] 本番DBを誤って上書きしないための厳格なパスチェック（`staging/` ディレクトリ配下であることを確認）を実装。
- [x] データの匿名化機能を実装。`content` および `description` カラムを伏せ字（例: `Staging Data`）に置換。
- [x] 集計に必要な数値データ（金額・日付・カテゴリ）は保持し、分析テストが可能な状態を維持。

## 担当エージェント
- @backend-engineer

## ステータス
- [x] 完了 (Closed)

## 完了コメント
匿名化スクリプトの開発を完了しました。本番環境のデータを安全にステージング環境へ持ち込めるようになり、プライバシーを保護しつつ高度な機能検証が可能になりました。
